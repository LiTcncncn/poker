---
description: Roguelike / VPoker 前端仅做手机界面适配，不做桌面断点布局
alwaysApply: true
---

# 界面范围：仅手机适配

本仓库 **Roguelike（`roguelike/`）及相关前端界面** 的布局与视觉 **只按手机竖屏** 设计与验收。

## 必须遵守

- **不要用响应式断点**（如 Tailwind `sm:`、`md:`、`lg:` 等）区分「桌面布局 vs 手机布局」或放大字号/留白；同一界面在宽屏浏览器上也应保持 **窄栏单列**（与游戏内 `max-w-sm` 量级一致），宁可两侧留白。
- **不要用桌面专属结构**：例如宽表格矩阵、多栏并排主导航；手机上用 **纵向分段 + 横向滑动（如 `overflow-x-auto`）** 代替宽表。
- **主应用内容区**使用 **固定最大逻辑宽度（`max-w-[390px]` 手机画布 + 水平居中）**，使 Mac 宽屏与真机看到 **同一列版心**；**禁止用手牌/技能牌上的 `vw` 定宽**（`vw` 随浏览器视口变，与版心脱钩），宽度应随父级或 `flex` 分配。
- **弹层**：优先 **底部对齐 / 全宽窄栏** 的手机习惯，避免依赖 `sm:items-center` 等大屏居中。
- 新增页面或改 UI 时，**先按 320～430px 有效宽度** 检视；`vw` / `max-w-sm` 等可用于约束，但不得为桌面加第二套主布局。

## 例外

- 仅在 **与手机无关** 的脚本、纯逻辑、无 UI 的模块中可忽略本规则。
- `prefers-reduced-motion` 等无障碍媒体查询允许使用。
